import * as React from "react";
import Typography, { TypographyProps } from "@mui/material/Typography";

export const CommonTypographyComponenet = (props: TypographyProps) => {
  return <Typography {...props} />;
};
