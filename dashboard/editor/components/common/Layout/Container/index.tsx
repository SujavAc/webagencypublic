import * as React from "react";
import Container, { ContainerProps } from "@mui/material/Container";

export const CommonContainerLayout = (props: ContainerProps) => {
  return <Container {...props} />;
};
